<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>ZhangZhengYu</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://welcomemonth.github.io/</id>
  <link href="https://welcomemonth.github.io/" rel="alternate"/>
  <link href="https://welcomemonth.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, ZhangZhengYu</rights>
  <subtitle>记录技术与生活</subtitle>
  <title>ZhangZhengYu 的博客</title>
  <updated>2026-04-18T08:00:00.000Z</updated>
  <entry>
    <author>
      <name>ZhangZhengYu</name>
    </author>
    <category term="AI工具" scheme="https://welcomemonth.github.io/categories/AI%E5%B7%A5%E5%85%B7/"/>
    <category term="Claude Code" scheme="https://welcomemonth.github.io/tags/Claude-Code/"/>
    <category term="教程" scheme="https://welcomemonth.github.io/tags/%E6%95%99%E7%A8%8B/"/>
    <category term="blog" scheme="https://welcomemonth.github.io/tags/blog/"/>
    <category term="vibcoding" scheme="https://welcomemonth.github.io/tags/vibcoding/"/>
    <content>
      <![CDATA[<h1 id="Claude-Code-配置全解析-让-AI-真正懂你的项目"><a href="#Claude-Code-配置全解析-让-AI-真正懂你的项目" class="headerlink" title="Claude Code 配置全解析:让 AI 真正懂你的项目"></a>Claude Code 配置全解析:让 AI 真正懂你的项目</h1><blockquote><p>从零开始，手把手教你搭建 Claude Code 的完整配置体系。告别每次重复解释项目背景的痛苦，让 AI 真正成为你团队的”老成员”。</p></blockquote><hr><h2 id="开篇：配置，才是-AI-编程的分水岭"><a href="#开篇：配置，才是-AI-编程的分水岭" class="headerlink" title="开篇：配置，才是 AI 编程的分水岭"></a>开篇：配置，才是 AI 编程的分水岭</h2><p>很多人第一次用 Claude Code 的体验是这样的：</p><blockquote><p>“帮我加个登录功能。”</p><p>AI 噼里啪啦改了一通，结果用了你项目里根本没有的框架，命名风格跟项目格格不入，还顺手 <code>console.log</code> 满天飞。</p><p>你花了 20 分钟纠正它，下次换个话题，它又忘了。</p></blockquote><p>这不是 AI 的问题——<strong>是你的配置没做好。</strong></p><p>打个比方：你招了一个技术很强的实习生，但他第一天上班，没人告诉他”我们用什么框架””代码怎么命名””哪些文件不能碰”。他只能靠自己猜，猜错了你还得返工。</p><p><strong>Claude Code 的配置系统，就是你给这个实习生准备的”入职手册”。</strong> 写好了，它秒懂项目背景、自动遵守规范、不再反复犯错。写不好，你就得天天跟在屁股后面收拾烂摊子。</p><p>本文会带你从零开始，把 Claude Code 的配置体系摸透。不需要任何前置知识——如果你已经用过 Claude Code，效果会更好；如果还没用过，跟着装也行。</p><hr><h2 id="第一部分：理解配置体系的全貌"><a href="#第一部分：理解配置体系的全貌" class="headerlink" title="第一部分：理解配置体系的全貌"></a>第一部分：理解配置体系的全貌</h2><p>在动手写配置之前，先花 5 分钟搞清楚整体结构。这就像装修之前先看户型图——方向对了，后面才不会装反。</p><h3 id="1-1-两个-claude-文件夹"><a href="#1-1-两个-claude-文件夹" class="headerlink" title="1.1 两个 .claude 文件夹"></a>1.1 两个 .claude 文件夹</h3><p>Claude Code 实际上有 <strong>两个</strong> <code>.claude</code> 目录，很多人只注意到一个：</p><table><thead><tr><th>文件夹</th><th>位置</th><th>谁用</th><th>是否提交到 Git</th></tr></thead><tbody><tr><td><strong>项目级</strong> <code>.claude/</code></td><td>你的项目根目录下</td><td>整个团队</td><td>✅ 是（团队共享）</td></tr><tr><td><strong>全局级</strong> <code>~/.claude/</code></td><td>你的用户主目录下</td><td>只有你自己</td><td>❌ 否（个人偏好）</td></tr></tbody></table><p><strong>项目级</strong> <code>.claude/</code> 存放的是”团队共识”：项目用什么技术栈、代码怎么写、哪些命令能跑。你把它提交到 Git，团队每个人 <code>git pull</code> 一下就获得相同的配置。</p><p><strong>全局级</strong> <code>~/.claude/</code> 存放的是”你的个人习惯”：你喜欢中文回复、你喜欢某种代码风格、你的会话历史。它只属于你，不会跟着项目走。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">你的电脑├── ~/.claude/                          ← 全局，跨所有项目│   ├── CLAUDE.md                       <span class="token comment"># 你的个人偏好</span>│   ├── settings.json                   <span class="token comment"># 全局权限设置</span>│   ├── skills/                         <span class="token comment"># 跨项目的个人 skills</span>│   ├── agents/                         <span class="token comment"># 跨项目的个人 agents</span>│   └── projects/                       <span class="token comment"># 会话历史 + 自动记忆</span>│└── 你的项目/    ├── CLAUDE.md                       ← 项目说明书（团队共享）    ├── CLAUDE.local.md                 ← 你的个人覆盖（不进 Git）    ├── .claudeignore                   ← 告诉 AI 忽略哪些文件    │    ├── .claude/                        ← 项目级配置    │   ├── settings.json               <span class="token comment"># 团队权限设置</span>    │   ├── settings.local.json         <span class="token comment"># 你的个人权限（不进 Git）</span>    │   ├── rules/                      <span class="token comment"># 模块化规则</span>    │   ├── skills/                     <span class="token comment"># 项目级 skills</span>    │   ├── agents/                     <span class="token comment"># 项目级 agents</span>    │   └── hooks/                      <span class="token comment"># 自动化脚本</span>    │    └── src/        ├── <span class="token punctuation">...</span>        └── payment/            └── CLAUDE.md               ← 模块说明书（仅该目录生效）<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="1-2-配置的优先级：谁说了算？"><a href="#1-2-配置的优先级：谁说了算？" class="headerlink" title="1.2 配置的优先级：谁说了算？"></a>1.2 配置的优先级：谁说了算？</h3><p>同一个设置可能出现在多个地方，Claude Code 按以下优先级加载（从高到低）：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">文件夹级 CLAUDE.md  →  项目级 CLAUDE.md  →  全局级 CLAUDE.md        ↓                   ↓                    ↓     最具体             团队共识             个人兜底<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><strong>高优先级覆盖低优先级。</strong> 比如全局 CLAUDE.md 写”用英文写注释”，项目级写”用中文写注释”，那么在当前项目中，Claude 会用中文。</p><p>同样的优先级规则也适用于 <code>settings.json</code>：项目级覆盖全局，<code>.local</code> 版本覆盖非 <code>.local</code> 版本。</p><h3 id="1-3-配置的分类：三层递进"><a href="#1-3-配置的分类：三层递进" class="headerlink" title="1.3 配置的分类：三层递进"></a>1.3 配置的分类：三层递进</h3><p>Claude Code 的配置可以分成三层，复杂度递增：</p><table><thead><tr><th>层级</th><th>包含什么</th><th>解决什么问题</th><th>学习曲线</th></tr></thead><tbody><tr><td><strong>基础层</strong></td><td>CLAUDE.md、settings.json、.claudeignore</td><td>让 AI 了解项目、控制权限</td><td>10 分钟上手</td></tr><tr><td><strong>进阶层</strong></td><td>rules/、hooks/、skills/、agents/</td><td>模块化指令、自动化工作流</td><td>需要一些实践经验</td></tr><tr><td><strong>记忆层</strong></td><td>Auto Memory、自建参考文档</td><td>长期记忆、按需加载</td><td>用着用着自然就懂了</td></tr></tbody></table><p>新手先把基础层搞定，后面按需逐步加入。别一上来就搞全套，会把自己搞晕。</p><hr><h2 id="第二部分：核心配置文件详解"><a href="#第二部分：核心配置文件详解" class="headerlink" title="第二部分：核心配置文件详解"></a>第二部分：核心配置文件详解</h2><p>这一部分是整篇文章的重中之重。掌握这几个文件，你就能解决 80% 的”AI 不听话”问题。</p><h3 id="2-1-CLAUDE-md：AI-的”项目入职手册”"><a href="#2-1-CLAUDE-md：AI-的”项目入职手册”" class="headerlink" title="2.1 CLAUDE.md：AI 的”项目入职手册”"></a>2.1 CLAUDE.md：AI 的”项目入职手册”</h3><p>如果说 Claude Code 只让你配一个文件，那一定是 CLAUDE.md。它是整个配置体系中<strong>投入产出比最高</strong>的一个。</p><h4 id="它是什么？"><a href="#它是什么？" class="headerlink" title="它是什么？"></a>它是什么？</h4><p>CLAUDE.md 是一个 Markdown 文件，Claude Code 每次启动时<strong>第一个读取</strong>它。它的内容会被直接注入到 AI 的系统提示词中，在整个对话过程中持续生效。</p><p>简单说：<strong>你在 CLAUDE.md 里写什么，Claude 就会照做。</strong></p><ul><li>你写”提交代码前必须先跑测试”——它每次都会跑测试</li><li>你写”不要使用 <code>console.log</code>，用项目里的 logger 模块”——它就不会再乱打印</li><li>你写”变量名用驼峰命名”——它就会遵守</li></ul><h4 id="三级-CLAUDE-md，各有分工"><a href="#三级-CLAUDE-md，各有分工" class="headerlink" title="三级 CLAUDE.md，各有分工"></a>三级 CLAUDE.md，各有分工</h4><p>很多人只知道在项目根目录放一个 CLAUDE.md。其实 Claude Code 设计了<strong>三个层级</strong>，同时生效、互不冲突：</p><table><thead><tr><th>层级</th><th>文件位置</th><th>作用范围</th><th>适合写什么</th><th>谁来维护</th></tr></thead><tbody><tr><td><strong>全局级</strong></td><td><code>~/.claude/CLAUDE.md</code></td><td>所有项目</td><td>个人偏好：语言、代码风格、通用原则</td><td>你自己</td></tr><tr><td><strong>项目级</strong></td><td><code>项目根目录/CLAUDE.md</code></td><td>当前项目</td><td>技术栈、架构、规范、进度</td><td>团队共享，提交到 Git</td></tr><tr><td><strong>文件夹级</strong></td><td><code>子目录/CLAUDE.md</code></td><td>仅该目录</td><td>模块专属约定，如支付模块的特殊规则</td><td>模块负责人</td></tr></tbody></table><p><strong>举个例子：</strong></p><ul><li>全局级写”永远用中文回复我”——所有项目都会用中文</li><li>项目级写”本项目使用 Next.js 14 + Prisma”——AI 就知道技术栈了</li><li><code>src/payment/CLAUDE.md</code> 写”支付金额一律用分为单位，不要用元”——只在支付模块下生效</li></ul><p>三层叠加，各司其职。</p><h4 id="CLAUDE-md-里该写什么？"><a href="#CLAUDE-md-里该写什么？" class="headerlink" title="CLAUDE.md 里该写什么？"></a>CLAUDE.md 里该写什么？</h4><p>这是新手最头疼的问题。写太少，AI 还是一脸懵；写太多，AI 反而记不住重点。</p><p><strong>✅ 该写的：</strong></p><table><thead><tr><th>内容</th><th>示例</th></tr></thead><tbody><tr><td>构建/测试/检查命令</td><td><code>npm run test</code>、<code>make build</code>、<code>npm run lint</code></td></tr><tr><td>技术栈和架构决策</td><td>“Express REST API, Node 20, PostgreSQL via Prisma”</td></tr><tr><td>不明显的注意事项</td><td>“TypeScript 开启了严格模式，未使用的变量会报错”</td></tr><tr><td>命名规范和导入约定</td><td>“组件用 PascalCase，工具函数用 camelCase”</td></tr><tr><td>目录结构概览</td><td>“所有 API handler 在 src/handlers/“</td></tr><tr><td>当前进度</td><td>“书签 CRUD API 开发中，搜索功能待开发”</td></tr><tr><td>禁忌事项</td><td>“不要修改数据库迁移文件”、”不要暴露堆栈信息给客户端”</td></tr></tbody></table><p><strong>❌ 不该写的：</strong></p><table><thead><tr><th>内容</th><th>为什么不该写</th></tr></thead><tbody><tr><td>Linter/Formatter 能管的事</td><td>“缩进用 2 空格” → 这是 ESLint 的工作</td></tr><tr><td>完整的 API 文档</td><td>太长，用 <code>@docs/api.md</code> 引用即可</td></tr><tr><td>大段的理论解释</td><td>AI 不需要你教它什么是 REST</td></tr><tr><td>重复的代码示例</td><td>给一个就够了，别贴十遍</td></tr></tbody></table><p><strong>黄金法则：CLAUDE.md 控制在 200 行以内。</strong> 超过这个长度，AI 的指令遵循度会明显下降。它就像一个入职手册——新人不会读 100 页的文档。</p><h4 id="一个可以直接用的模板"><a href="#一个可以直接用的模板" class="headerlink" title="一个可以直接用的模板"></a>一个可以直接用的模板</h4><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token title important"><span class="token punctuation">#</span> 项目名称</span><span class="token title important"><span class="token punctuation">##</span> 常用命令</span>npm run dev          # 启动开发服务器npm run test         # 运行测试（Jest）npm run lint         # 代码检查（ESLint + Prettier）npm run build        # 生产构建<span class="token title important"><span class="token punctuation">##</span> 架构</span><span class="token list punctuation">-</span> Express REST API，Node 20<span class="token list punctuation">-</span> PostgreSQL，通过 Prisma ORM 操作<span class="token list punctuation">-</span> 所有 API handler 在 src/handlers/<span class="token list punctuation">-</span> 共享类型定义在 src/types/<span class="token title important"><span class="token punctuation">##</span> 编码规范</span><span class="token list punctuation">-</span> 每个 handler 用 zod 做请求校验<span class="token list punctuation">-</span> 返回值统一格式：{ data, error }<span class="token list punctuation">-</span> 不向客户端暴露内部错误详情<span class="token list punctuation">-</span> 使用项目内的 logger 模块，不用 console.log<span class="token title important"><span class="token punctuation">##</span> 注意事项</span><span class="token list punctuation">-</span> 测试用的是真实本地数据库，不是 mock。跑测试前先 <span class="token code-snippet code keyword">`npm run db:test:reset`</span><span class="token list punctuation">-</span> TypeScript 严格模式：不允许未使用的 import<span class="token list punctuation">-</span> 不要修改 prisma/migrations/ 下的文件<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>大约 20 行。简洁、具体、可执行。直接复制到你的项目里，按实际情况改就行。</p><h4 id="如何创建-CLAUDE-md？"><a href="#如何创建-CLAUDE-md？" class="headerlink" title="如何创建 CLAUDE.md？"></a>如何创建 CLAUDE.md？</h4><p><strong>方法一：用 <code>/init</code> 命令自动生成（推荐新手）</strong></p><p>在项目根目录启动 Claude Code，输入 <code>/init</code>。它会自动扫描你的项目，生成一份 CLAUDE.md 初稿。你在这基础上删减修改即可。</p><blockquote><p>💡 <strong>提示</strong>：项目有一定规模再用 <code>/init</code> 效果更好。如果项目刚初始化，什么文件都没有，它扫不出什么东西。</p></blockquote><p><strong>方法二：用 <code>/memory</code> 编辑全局级</strong></p><p>在 Claude Code 会话中输入 <code>/memory</code>，选择”全局 CLAUDE.md”，会用默认编辑器打开 <code>~/.claude/CLAUDE.md</code>。修改后<strong>需要重启 Claude Code 才生效</strong>。</p><p><strong>方法三：手动创建</strong></p><p>直接在项目根目录新建 <code>CLAUDE.md</code> 文件，按上面的模板写。</p><h4 id="CLAUDE-local-md：你的个人小抄"><a href="#CLAUDE-local-md：你的个人小抄" class="headerlink" title="CLAUDE.local.md：你的个人小抄"></a>CLAUDE.local.md：你的个人小抄</h4><p>有时候你有个人的偏好，但不想让团队其他人也被影响。比如你习惯用另一种测试运行器，或者你想临时让 Claude 输出更详细的注释。</p><p>在项目根目录创建 <code>CLAUDE.local.md</code>。Claude Code 会连同主 CLAUDE.md 一起读取它，而且它会被<strong>自动加入 .gitignore</strong>——你的个人调整永远不会进入团队仓库。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">CLAUDE.md           → 团队共享，提交到 GitCLAUDE.local.md     → 你的个人覆盖，不进 Git<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><hr><h3 id="2-2-settings-json：权限的”门禁系统”"><a href="#2-2-settings-json：权限的”门禁系统”" class="headerlink" title="2.2 settings.json：权限的”门禁系统”"></a>2.2 settings.json：权限的”门禁系统”</h3><p>CLAUDE.md 告诉 AI “做什么”，settings.json 告诉 AI “能做什么、不能做什么”。</p><p>它是 Claude Code 的权限门禁系统：哪些操作可以直接执行、哪些需要你确认、哪些完全禁止。</p><h4 id="三个层级的-settings-json"><a href="#三个层级的-settings-json" class="headerlink" title="三个层级的 settings.json"></a>三个层级的 settings.json</h4><p>跟 CLAUDE.md 类似，settings.json 也有三个层级：</p><table><thead><tr><th>层级</th><th>文件位置</th><th>作用范围</th><th>提交到 Git？</th></tr></thead><tbody><tr><td><strong>全局</strong></td><td><code>~/.claude/settings.json</code></td><td>所有项目</td><td>❌</td></tr><tr><td><strong>项目（团队）</strong></td><td><code>项目/.claude/settings.json</code></td><td>当前项目</td><td>✅</td></tr><tr><td><strong>项目（个人）</strong></td><td><code>项目/.claude/settings.local.json</code></td><td>当前项目</td><td>❌（自动 gitignore）</td></tr></tbody></table><p>优先级：个人 &gt; 项目团队 &gt; 全局。</p><h4 id="权限配置详解"><a href="#权限配置详解" class="headerlink" title="权限配置详解"></a>权限配置详解</h4><p>settings.json 的核心就是 <code>permissions</code> 字段，它有两个列表：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"$schema"</span><span class="token operator">:</span> <span class="token string">"https://json.schemastore.org/claude-code-settings.json"</span><span class="token punctuation">,</span>  <span class="token property">"permissions"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"allow"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token string">"Read"</span><span class="token punctuation">,</span>      <span class="token string">"Write"</span><span class="token punctuation">,</span>      <span class="token string">"Edit"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(npm run *)"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(git status)"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(git diff *)"</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"deny"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token string">"Bash(rm -rf *)"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(curl *)"</span><span class="token punctuation">,</span>      <span class="token string">"Read(./.env)"</span><span class="token punctuation">,</span>      <span class="token string">"Read(./.env.*)"</span>    <span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong><code>allow</code> 列表——免通行证</strong></p><p>写在里面的操作，Claude Code 执行时<strong>不会问你确认</strong>，直接放行。</p><p>常见的 allow 配置：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token property">"allow"</span><span class="token operator">:</span> <span class="token punctuation">[</span>  <span class="token string">"Read"</span><span class="token punctuation">,</span>                  <span class="token comment">// 允许读文件</span>  <span class="token string">"Write"</span><span class="token punctuation">,</span>                 <span class="token comment">// 允许写文件</span>  <span class="token string">"Edit"</span><span class="token punctuation">,</span>                  <span class="token comment">// 允许编辑文件</span>  <span class="token string">"Bash(npm run *)"</span><span class="token punctuation">,</span>       <span class="token comment">// 允许跑 npm 脚本</span>  <span class="token string">"Bash(make *)"</span><span class="token punctuation">,</span>          <span class="token comment">// 允许跑 make 命令</span>  <span class="token string">"Bash(git status)"</span><span class="token punctuation">,</span>      <span class="token comment">// 允许查看 git 状态</span>  <span class="token string">"Bash(git diff *)"</span><span class="token punctuation">,</span>      <span class="token comment">// 允许查看 diff</span>  <span class="token string">"Bash(git log *)"</span><span class="token punctuation">,</span>       <span class="token comment">// 允许查看日志</span>  <span class="token string">"Glob"</span><span class="token punctuation">,</span>                  <span class="token comment">// 允许搜索文件</span>  <span class="token string">"Grep"</span>                   <span class="token comment">// 允许搜索内容</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>💡 <code>Bash(npm run *)</code> 中的 <code>*</code> 是通配符，匹配任意字符。所以 <code>npm run test</code>、<code>npm run build</code>、<code>npm run dev</code> 都会被放行，但 <code>npm publish</code> 不会——它不是 <code>npm run</code> 开头。</p></blockquote><p><strong><code>deny</code> 列表——黑名单</strong></p><p>写在里面的操作，<strong>无论什么情况都不允许执行</strong>，即使你手动确认也不行。</p><p>一个合理的 deny 列表应该包含：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token property">"deny"</span><span class="token operator">:</span> <span class="token punctuation">[</span>  <span class="token string">"Bash(rm -rf *)"</span><span class="token punctuation">,</span>            <span class="token comment">// 禁止危险删除</span>  <span class="token string">"Bash(git push --force *)"</span><span class="token punctuation">,</span>  <span class="token comment">// 禁止强制推送</span>  <span class="token string">"Bash(curl *)"</span><span class="token punctuation">,</span>              <span class="token comment">// 禁止直接网络请求</span>  <span class="token string">"Bash(wget *)"</span><span class="token punctuation">,</span>              <span class="token comment">// 禁止下载</span>  <span class="token string">"Read(./.env)"</span><span class="token punctuation">,</span>              <span class="token comment">// 禁止读取环境变量文件</span>  <span class="token string">"Read(./.env.*)"</span><span class="token punctuation">,</span>            <span class="token comment">// 禁止读取所有 .env 变体</span>  <span class="token string">"Read(./secrets/**)"</span>         <span class="token comment">// 禁止读取 secrets 目录</span><span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>不在任何列表中的操作——灰色地带</strong></p><p>如果某个操作既不在 allow 也不在 deny 中，Claude Code 会<strong>在执行前先问你</strong>。这就是”灰色地带”——安全网。</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">AI</span><span class="token punctuation">:</span> 我要执行 `pip install requests`，可以吗？<span class="token key atrule">你</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>确认<span class="token punctuation">]</span> / <span class="token punctuation">[</span>拒绝<span class="token punctuation">]</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这个中间地带是有意设计的——你不需要提前想到所有可能的命令。</p><h4 id="其他常用配置项"><a href="#其他常用配置项" class="headerlink" title="其他常用配置项"></a>其他常用配置项</h4><p>除了权限，settings.json 还能配置这些：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"$schema"</span><span class="token operator">:</span> <span class="token string">"https://json.schemastore.org/claude-code-settings.json"</span><span class="token punctuation">,</span>  <span class="token property">"permissions"</span><span class="token operator">:</span> <span class="token punctuation">{</span> ... <span class="token punctuation">}</span><span class="token punctuation">,</span>  <span class="token comment">// 默认使用的模型</span>  <span class="token property">"model"</span><span class="token operator">:</span> <span class="token string">"sonnet"</span><span class="token punctuation">,</span>  <span class="token comment">// 上下文自动压缩阈值（0-100，百分比）</span>  <span class="token comment">// 上下文占用超过这个比例时自动压缩</span>  <span class="token property">"autoCompactThreshold"</span><span class="token operator">:</span> <span class="token number">80</span><span class="token punctuation">,</span>  <span class="token comment">// 环境变量（在 Claude Code 会话中可用）</span>  <span class="token property">"env"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"NODE_ENV"</span><span class="token operator">:</span> <span class="token string">"development"</span><span class="token punctuation">,</span>    <span class="token property">"API_BASE_URL"</span><span class="token operator">:</span> <span class="token string">"http://localhost:3000"</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>💡 <strong><code>$schema</code> 那行很重要</strong>：加上它，VS Code 或 Cursor 会自动提供配置项的自动补全和校验，能省掉很多拼写错误。</p></blockquote><h4 id="新手建议"><a href="#新手建议" class="headerlink" title="新手建议"></a>新手建议</h4><p>刚入门时，建议保持<strong>最保守的权限配置</strong>：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"permissions"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"deny"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token string">"Bash(rm -rf *)"</span><span class="token punctuation">,</span>      <span class="token string">"Read(./.env)"</span>    <span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>只配 deny，不配 allow。这样 Claude Code 每次执行操作都会问你确认。等你用熟了，再逐步把信任的操作加入 allow 列表。</p><hr><h3 id="2-3-claudeignore：给-AI-戴上”眼罩”"><a href="#2-3-claudeignore：给-AI-戴上”眼罩”" class="headerlink" title="2.3 .claudeignore：给 AI 戴上”眼罩”"></a>2.3 .claudeignore：给 AI 戴上”眼罩”</h3><p>你的项目里有很多文件是 AI 根本不需要看的：<code>node_modules/</code> 有几万个文件、<code>dist/</code> 是构建产物、<code>.env</code> 里是密钥。让 AI 扫描这些文件，只会浪费 token、拖慢速度，甚至可能泄露敏感信息。</p><p><code>.claudeignore</code> 就像给 AI 戴上一个”眼罩”——告诉它哪些东西不要看。</p><h4 id="语法跟-gitignore-一样"><a href="#语法跟-gitignore-一样" class="headerlink" title="语法跟 .gitignore 一样"></a>语法跟 .gitignore 一样</h4><p>如果你用过 <code>.gitignore</code>，<code>.claudeignore</code> 的语法完全一样：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># 依赖包目录（太大了，不需要看）</span>node_modules/<span class="token comment"># 构建产物</span>dist/build/.next/<span class="token comment"># 日志文件</span><span class="token important">*.log</span><span class="token comment"># 敏感文件</span>.env.env.*secrets/<span class="token comment"># 数据库文件</span><span class="token important">*.db</span><span class="token important">*.sqlite</span><span class="token comment"># 系统文件</span>.DS_StoreThumbs.db<span class="token comment"># IDE 配置</span>.vscode/.idea/<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="为什么这很重要？"><a href="#为什么这很重要？" class="headerlink" title="为什么这很重要？"></a>为什么这很重要？</h4><ol><li><strong>省 token</strong>：<code>node_modules/</code> 随便就几万文件，让 AI 扫描纯属浪费</li><li><strong>防泄露</strong>：<code>.env</code> 里可能有数据库密码、API Key，不能让 AI 读到</li><li><strong>提速度</strong>：排除的文件越多，AI 搜索越快</li><li><strong>减噪音</strong>：构建产物、日志文件对 AI 来说是垃圾信息</li></ol><blockquote><p>⚠️ <strong>红线</strong>：<code>.env</code>、密钥文件、证书文件一定要加到 <code>.claudeignore</code> 里。这是安全底线，不是可选项。</p></blockquote><hr><h3 id="2-4-常用斜杠命令速查"><a href="#2-4-常用斜杠命令速查" class="headerlink" title="2.4 常用斜杠命令速查"></a>2.4 常用斜杠命令速查</h3><p>配置好之后，日常使用中这几个命令你会天天用到：</p><table><thead><tr><th>命令</th><th>作用</th><th>什么时候用</th></tr></thead><tbody><tr><td><code>/init</code></td><td>自动生成 CLAUDE.md</td><td>进入新项目的第一件事</td></tr><tr><td><code>/memory</code></td><td>查看/编辑记忆和 CLAUDE.md</td><td>管理项目说明和自动记忆</td></tr><tr><td><code>/context</code></td><td>查看上下文使用情况</td><td>感觉 AI “变笨”了，看看上下文是不是满了</td></tr><tr><td><code>/compact</code></td><td>压缩上下文</td><td>对话太长，AI 开始”遗忘”时</td></tr><tr><td><code>/clear</code></td><td>清空对话，重新开始</td><td>一个任务完成，开始全新任务</td></tr><tr><td><code>/model</code></td><td>切换模型</td><td>需要更强/更快的模型时</td></tr><tr><td><code>/rewind</code></td><td>回滚修改</td><td>AI 改坏了代码时的”后悔药”</td></tr><tr><td><code>/review</code></td><td>代码审查</td><td>完成开发后检查质量</td></tr><tr><td><code>/help</code></td><td>查看所有命令</td><td>忘了有什么命令时</td></tr></tbody></table><blockquote><p>💡 <strong><code>/compact</code> vs <code>/clear</code></strong>：</p><ul><li><code>/compact</code> = 整理桌面，把前面的对话压缩成摘要，还能继续当前任务</li><li><code>/clear</code> = 把桌面清空，等于重新开始</li></ul><p>经验法则：看到上下文使用超过 60%，就 <code>/compact</code> 一下。一个独立任务完成后，用 <code>/clear</code> 开始新的。</p></blockquote><hr><h2 id="第三部分：进阶配置系统"><a href="#第三部分：进阶配置系统" class="headerlink" title="第三部分：进阶配置系统"></a>第三部分：进阶配置系统</h2><p>基础配置搞定之后，你会发现有些需求用 CLAUDE.md 和 settings.json 解决不了——</p><ul><li>CLAUDE.md 越来越长，300 行了，根本维护不动 → 需要 <strong>rules/</strong></li><li>想让 AI 改完文件自动跑格式化，而不是靠它”记得” → 需要 <strong>hooks/</strong></li><li>有一整套安全审查流程，想打包给 AI 用 → 需要 <strong>skills/</strong></li><li>想让一个”专家角色”在隔离环境里做代码审查 → 需要 <strong>agents/</strong></li></ul><p>这一部分介绍的四个机制，是 Claude Code 从”工具”进化为”团队成员”的关键。</p><h3 id="3-1-rules-文件夹：模块化指令"><a href="#3-1-rules-文件夹：模块化指令" class="headerlink" title="3.1 rules/ 文件夹：模块化指令"></a>3.1 rules/ 文件夹：模块化指令</h3><h4 id="问题：CLAUDE-md-越来越臃肿"><a href="#问题：CLAUDE-md-越来越臃肿" class="headerlink" title="问题：CLAUDE.md 越来越臃肿"></a>问题：CLAUDE.md 越来越臃肿</h4><p>项目刚起步时，一个 CLAUDE.md 就够了。但随着团队壮大，你会发现：</p><ul><li>前端、后端、测试各有不同的规范</li><li>API 约定、安全规则、性能要求各不相同</li><li>一个人在维护一份 300 行的 CLAUDE.md，谁都不敢改</li></ul><p><strong>解决方案：把指令拆分成多个文件，放在 <code>.claude/rules/</code> 目录下。</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">.claude/rules/├── code<span class="token punctuation">-</span>style.md        <span class="token comment"># 代码风格</span>├── testing.md           <span class="token comment"># 测试规范</span>├── api<span class="token punctuation">-</span>conventions.md   <span class="token comment"># API 约定</span>└── security.md          <span class="token comment"># 安全规则</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>Claude Code 会<strong>自动加载</strong> rules/ 下的每个 Markdown 文件，跟 CLAUDE.md 一起生效。每个文件保持聚焦、易于维护。负责 API 规范的团队成员改 <code>api-conventions.md</code>，负责测试的人改 <code>testing.md</code>，互不干扰。</p><h4 id="杀手级功能：路径作用域"><a href="#杀手级功能：路径作用域" class="headerlink" title="杀手级功能：路径作用域"></a>杀手级功能：路径作用域</h4><p>rules/ 真正的威力在于——你可以让一个规则<strong>只在特定目录下生效</strong>。</p><p>做法很简单，在规则文件开头加一段 YAML frontmatter：</p><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token front-matter-block"><span class="token punctuation">---</span><span class="token front-matter yaml language-yaml"><span class="token key atrule">paths</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token string">"src/api/**/*.ts"</span>  <span class="token punctuation">-</span> <span class="token string">"src/handlers/**/*.ts"</span></span><span class="token punctuation">---</span></span><span class="token title important"><span class="token punctuation">#</span> API 设计规范</span><span class="token list punctuation">-</span> 所有 handler 返回 { data, error } 格式<span class="token list punctuation">-</span> 用 zod 做请求体校验<span class="token list punctuation">-</span> 永远不向客户端暴露内部错误详情<span class="token list punctuation">-</span> 分页参数统一用 page + pageSize<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>效果：Claude Code 在编辑 <code>src/api/</code> 或 <code>src/handlers/</code> 下的文件时，才会加载这个规则。编辑 React 组件时，不会加载——节省 token，也避免不相关的规则干扰 AI。</p><p><strong>路径匹配的语法：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">"src/api/<span class="token important">**/*.ts"</span>     → src/api/ 下所有层级的 .ts 文件"<span class="token important">*.test.ts"</span>           → 所有 .test.ts 文件"docs/<span class="token important">**"</span>             → docs/ 下所有文件<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><strong>没有 paths 字段的规则文件</strong>会在每次会话中无条件加载——适合那些全局通用的规则。</p><h4 id="什么时候该用-rules-？"><a href="#什么时候该用-rules-？" class="headerlink" title="什么时候该用 rules/？"></a>什么时候该用 rules/？</h4><table><thead><tr><th>场景</th><th>建议</th></tr></thead><tbody><tr><td>CLAUDE.md 超过 100 行了</td><td>按关注点拆分到 rules/</td></tr><tr><td>不同目录有不同规范</td><td>用路径作用域</td></tr><tr><td>团队成员各自维护各自领域</td><td>拆成独立文件，分工维护</td></tr><tr><td>项目还小，CLAUDE.md 只有 30 行</td><td>先不用 rules/，别过度设计</td></tr></tbody></table><hr><h3 id="3-2-hooks-系统：确定性行为控制"><a href="#3-2-hooks-系统：确定性行为控制" class="headerlink" title="3.2 hooks/ 系统：确定性行为控制"></a>3.2 hooks/ 系统：确定性行为控制</h3><h4 id="CLAUDE-md-的局限：它只是”建议”"><a href="#CLAUDE-md-的局限：它只是”建议”" class="headerlink" title="CLAUDE.md 的局限：它只是”建议”"></a>CLAUDE.md 的局限：它只是”建议”</h4><p>CLAUDE.md 里写的规则，AI <strong>大多数时候</strong>会遵守，但<strong>不是每次</strong>。</p><ul><li>你写”改完文件后跑 prettier 格式化”——AI 有时会忘</li><li>你写”不要执行 <code>rm -rf</code>“——万一 AI 理解错了呢？</li><li>你写”完成之前必须通过测试”——AI 可能会跳过测试直接宣布完成</li></ul><p>语言模型就是这样——它尽力遵守，但你没法 100% 保证。</p><p><strong>hooks 让你的规则从”建议”变成”强制执行”。</strong></p><h4 id="hooks-是什么？"><a href="#hooks-是什么？" class="headerlink" title="hooks 是什么？"></a>hooks 是什么？</h4><p>hooks 是<strong>事件处理器</strong>——在 Claude Code 工作流的特定节点，自动执行你写的 shell 脚本。你的脚本每次都会运行，没有例外。</p><p>这就像工厂流水线上的质检站：产品（AI 的操作）经过每个站点时，质检员（你的脚本）自动检查。不合格？直接打回。</p><h4 id="支持的事件类型"><a href="#支持的事件类型" class="headerlink" title="支持的事件类型"></a>支持的事件类型</h4><table><thead><tr><th>事件</th><th>触发时机</th><th>典型用途</th></tr></thead><tbody><tr><td><strong>PreToolUse</strong></td><td>工具执行<strong>前</strong></td><td>安全检查：阻止危险命令</td></tr><tr><td><strong>PostToolUse</strong></td><td>工具执行<strong>后</strong></td><td>自动格式化、自动 lint</td></tr><tr><td><strong>Stop</strong></td><td>AI 宣布完成时</td><td>质量关卡：测试必须通过</td></tr><tr><td><strong>UserPromptSubmit</strong></td><td>用户按回车时</td><td>输入验证、敏感词过滤</td></tr><tr><td><strong>Notification</strong></td><td>需要通知时</td><td>桌面弹窗提醒</td></tr><tr><td><strong>SessionStart</strong></td><td>会话开始</td><td>注入上下文</td></tr><tr><td><strong>SessionEnd</strong></td><td>会话结束</td><td>清理资源</td></tr></tbody></table><h4 id="退出码：脚本如何”告诉”-Claude-Code-该怎么做"><a href="#退出码：脚本如何”告诉”-Claude-Code-该怎么做" class="headerlink" title="退出码：脚本如何”告诉” Claude Code 该怎么做"></a>退出码：脚本如何”告诉” Claude Code 该怎么做</h4><p>这是 hooks 最关键的机制。你的脚本执行完后，通过<strong>退出码</strong>来告诉 Claude Code 下一步怎么做：</p><table><thead><tr><th>退出码</th><th>含义</th><th>Claude Code 的反应</th></tr></thead><tbody><tr><td><strong>0</strong></td><td>成功</td><td>继续正常执行</td></tr><tr><td><strong>1</strong></td><td>错误，但不阻止</td><td>记录日志，<strong>继续执行</strong>（不阻止！）</td></tr><tr><td><strong>2</strong></td><td>严重问题，必须阻止</td><td><strong>停止执行</strong>，把 stderr 的内容发给 AI 让它自我纠正</td></tr></tbody></table><blockquote><p>⚠️ <strong>新手最常犯的错误</strong>：在安全 hook 里用退出码 1，以为能阻止危险操作。实际上退出码 1 <strong>不会阻止执行</strong>！必须用退出码 2 才能真正阻止。</p></blockquote><h4 id="配置示例"><a href="#配置示例" class="headerlink" title="配置示例"></a>配置示例</h4><p>所有 hooks 配置在 <code>settings.json</code> 的 <code>hooks</code> 字段下：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"hooks"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"PostToolUse"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token punctuation">{</span>        <span class="token property">"matcher"</span><span class="token operator">:</span> <span class="token string">"Write|Edit|MultiEdit"</span><span class="token punctuation">,</span>        <span class="token property">"hooks"</span><span class="token operator">:</span> <span class="token punctuation">[</span>          <span class="token punctuation">{</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"command"</span><span class="token punctuation">,</span>            <span class="token property">"command"</span><span class="token operator">:</span> <span class="token string">"jq -r '.tool_input.file_path' | xargs npx prettier --write 2&gt;/dev/null"</span>          <span class="token punctuation">}</span>        <span class="token punctuation">]</span>      <span class="token punctuation">}</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"PreToolUse"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token punctuation">{</span>        <span class="token property">"matcher"</span><span class="token operator">:</span> <span class="token string">"Bash"</span><span class="token punctuation">,</span>        <span class="token property">"hooks"</span><span class="token operator">:</span> <span class="token punctuation">[</span>          <span class="token punctuation">{</span>            <span class="token property">"type"</span><span class="token operator">:</span> <span class="token string">"command"</span><span class="token punctuation">,</span>            <span class="token property">"command"</span><span class="token operator">:</span> <span class="token string">"$CLAUDE_PROJECT_DIR/.claude/hooks/bash-firewall.sh"</span>          <span class="token punctuation">}</span>        <span class="token punctuation">]</span>      <span class="token punctuation">}</span>    <span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>来拆解一下这段配置：</p><p><strong>第一个 hook（PostToolUse）</strong>：</p><ul><li>触发时机：<code>Write</code>、<code>Edit</code> 或 <code>MultiEdit</code> 工具执行<strong>之后</strong></li><li>做什么：用 prettier 自动格式化被修改的文件</li><li>效果：Claude Code 每次修改文件后，代码自动格式化</li></ul><p><strong>第二个 hook（PreToolUse）</strong>：</p><ul><li>触发时机：<code>Bash</code> 工具执行<strong>之前</strong></li><li>做什么：运行 <code>bash-firewall.sh</code> 脚本，检查命令是否安全</li><li>效果：危险命令被拦截</li></ul><blockquote><p>💡 <code>matcher</code> 字段是正则表达式，用来筛选哪些工具触发这个 hook。<code>"Write|Edit|MultiEdit"</code> 匹配文件修改操作，<code>"Bash"</code> 匹配终端命令。不写 matcher 则匹配所有工具。</p></blockquote><h4 id="实战：写一个-bash-防火墙"><a href="#实战：写一个-bash-防火墙" class="headerlink" title="实战：写一个 bash 防火墙"></a>实战：写一个 bash 防火墙</h4><p><code>.claude/hooks/bash-firewall.sh</code> 的内容：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token shebang important">#!/bin/bash</span><span class="token comment"># 从 stdin 读取 JSON 输入，提取要执行的命令</span><span class="token assign-left variable">INPUT</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span><span class="token function">cat</span><span class="token variable">)</span></span><span class="token assign-left variable">COMMAND</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span><span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$INPUT</span>"</span> <span class="token operator">|</span> jq <span class="token parameter variable">-r</span> <span class="token string">'.tool_input.command // empty'</span><span class="token variable">)</span></span><span class="token comment"># 检查危险模式</span><span class="token keyword">if</span> <span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$COMMAND</span>"</span> <span class="token operator">|</span> <span class="token function">grep</span> <span class="token parameter variable">-qE</span> <span class="token string">'rm -rf /|git push --force main|DROP TABLE'</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"⛔ 危险命令被拦截: <span class="token variable">$COMMAND</span>"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span>  <span class="token builtin class-name">exit</span> <span class="token number">2</span>  <span class="token comment"># 退出码 2 = 阻止执行！</span><span class="token keyword">fi</span><span class="token builtin class-name">exit</span> <span class="token number">0</span>  <span class="token comment"># 退出码 0 = 放行</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当 Claude Code 要执行 <code>rm -rf /</code> 时：</p><ol><li>PreToolUse hook 触发</li><li>脚本检查到危险模式</li><li>以退出码 2 退出，stderr 输出拦截信息</li><li>Claude Code 停止执行，把拦截信息发给 AI</li><li>AI 收到”危险命令被拦截”的反馈，换个安全的方式</li></ol><h4 id="实战：测试必须通过才能完成"><a href="#实战：测试必须通过才能完成" class="headerlink" title="实战：测试必须通过才能完成"></a>实战：测试必须通过才能完成</h4><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token shebang important">#!/bin/bash</span><span class="token comment"># .claude/hooks/enforce-tests.sh</span><span class="token comment"># 避免无限循环：检查 stop_hook_active 标志</span><span class="token assign-left variable">INPUT</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span><span class="token function">cat</span><span class="token variable">)</span></span><span class="token keyword">if</span> <span class="token builtin class-name">echo</span> <span class="token string">"<span class="token variable">$INPUT</span>"</span> <span class="token operator">|</span> jq <span class="token parameter variable">-e</span> <span class="token string">'.stop_hook_active'</span> <span class="token operator">&gt;</span> /dev/null <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span><span class="token file-descriptor important">&amp;1</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">exit</span> <span class="token number">0</span>  <span class="token comment"># 第二次触发了，放行，让 AI 停下来</span><span class="token keyword">fi</span><span class="token comment"># 跑测试</span><span class="token function">npm</span> <span class="token builtin class-name">test</span> <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span><span class="token file-descriptor important">&amp;1</span><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token variable">$?</span> <span class="token parameter variable">-ne</span> <span class="token number">0</span> <span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token keyword">then</span>  <span class="token builtin class-name">echo</span> <span class="token string">"❌ 测试未通过，请修复后再完成"</span> <span class="token operator">&gt;</span><span class="token file-descriptor important">&amp;2</span>  <span class="token builtin class-name">exit</span> <span class="token number">2</span>  <span class="token comment"># 阻止 AI 宣布完成</span><span class="token keyword">fi</span><span class="token builtin class-name">exit</span> <span class="token number">0</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>⚠️ <strong>注意 <code>stop_hook_active</code> 标志</strong>：如果不检查它，会出现无限循环——hook 阻止 AI → AI 重试 → hook 又阻止 → AI 又重试……检查这个标志，第二次触发时放行，让 AI 停下来。</p></blockquote><h4 id="hooks-的几个坑"><a href="#hooks-的几个坑" class="headerlink" title="hooks 的几个坑"></a>hooks 的几个坑</h4><ol><li><strong>不会热重载</strong>：修改了 hooks 配置，需要重启 Claude Code 才生效</li><li><strong>PostToolUse 无法撤销</strong>：工具已经执行了，PostToolUse 只能”事后处理”。要阻止操作，用 PreToolUse</li><li><strong>子 agent 也会触发</strong>：hooks 对子 agent 的操作也会递归触发</li><li><strong>没有沙箱</strong>：hooks 脚本以你的完整用户权限运行。务必引号包裹变量、校验 JSON 输入、脚本引用用绝对路径</li></ol><hr><h3 id="3-3-skills-文件夹：可复用的工作流"><a href="#3-3-skills-文件夹：可复用的工作流" class="headerlink" title="3.3 skills/ 文件夹：可复用的工作流"></a>3.3 skills/ 文件夹：可复用的工作流</h3><h4 id="什么是-skills？"><a href="#什么是-skills？" class="headerlink" title="什么是 skills？"></a>什么是 skills？</h4><p>想象你经常需要做安全审查。每次你都要告诉 AI：”检查 SQL 注入、XSS、暴露的密钥、不安全的配置……” 说了十遍之后，你烦了。</p><p><strong>Skills 就是你把这些重复的工作流”打包”起来，让 AI 在合适的时候自动调用。</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">你</span><span class="token punctuation">:</span> <span class="token string">"帮我审查一下这个 PR 的安全问题"</span>AI 识别到匹配 security<span class="token punctuation">-</span>review skill → 自动调用 → 按你定义好的流程审查<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h4 id="skill-的目录结构"><a href="#skill-的目录结构" class="headerlink" title="skill 的目录结构"></a>skill 的目录结构</h4><p>每个 skill 是一个独立的子目录，包含一个 <code>SKILL.md</code> 文件：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">.claude/skills/├── security<span class="token punctuation">-</span>review/│   ├── SKILL.md              <span class="token comment"># 触发条件和指令</span>│   └── DETAILED_GUIDE.md     <span class="token comment"># 详细的安全标准文档</span>└── deploy/    ├── SKILL.md              <span class="token comment"># 部署流程</span>    └── templates/        └── release<span class="token punctuation">-</span>notes.md  <span class="token comment"># 发布说明模板</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>跟 commands（单个文件）不同，skills 可以<strong>打包多个支持文件</strong>。这是它最大的优势。</p><h4 id="SKILL-md-怎么写？"><a href="#SKILL-md-怎么写？" class="headerlink" title="SKILL.md 怎么写？"></a>SKILL.md 怎么写？</h4><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token front-matter-block"><span class="token punctuation">---</span><span class="token front-matter yaml language-yaml"><span class="token key atrule">name</span><span class="token punctuation">:</span> security<span class="token punctuation">-</span>review<span class="token key atrule">description</span><span class="token punctuation">:</span> 全面的安全审查。在审查代码漏洞、部署前检查、  或用户提到安全相关话题时使用。<span class="token key atrule">allowed-tools</span><span class="token punctuation">:</span> Read<span class="token punctuation">,</span> Grep<span class="token punctuation">,</span> Glob</span><span class="token punctuation">---</span></span>对代码库进行安全漏洞分析：<span class="token list punctuation">1.</span> SQL 注入和 XSS 风险<span class="token list punctuation">2.</span> 暴露的凭据或密钥<span class="token list punctuation">3.</span> 不安全的配置<span class="token list punctuation">4.</span> 认证和授权的漏洞输出带严重等级的发现报告，以及具体的修复建议。参考 @DETAILED_GUIDE.md 了解我们的安全标准。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>关键字段解释：</strong></p><table><thead><tr><th>字段</th><th>作用</th><th>示例</th></tr></thead><tbody><tr><td><code>name</code></td><td>skill 名称</td><td><code>security-review</code></td></tr><tr><td><code>description</code></td><td>什么时候触发这个 skill</td><td>“在审查安全漏洞时使用”</td></tr><tr><td><code>allowed-tools</code></td><td>限制 skill 能使用的工具</td><td><code>Read, Grep, Glob</code>（只读，不写文件）</td></tr></tbody></table><blockquote><p>💡 <code>@DETAILED_GUIDE.md</code> 中的 <code>@</code> 符号会引入同目录下的文件。skill 执行时，这个文件的内容也会被加载。这就是”打包”的含义——指令和支持文件一起。</p></blockquote><h4 id="两种调用方式"><a href="#两种调用方式" class="headerlink" title="两种调用方式"></a>两种调用方式</h4><ol><li><strong>自动调用</strong>：你说”审查一下安全问题”，Claude 读取 description，发现匹配，自动调用 skill</li><li><strong>手动调用</strong>：在对话框输入 <code>/security-review</code>，直接触发</li></ol><h4 id="个人-skills-vs-项目-skills"><a href="#个人-skills-vs-项目-skills" class="headerlink" title="个人 skills vs 项目 skills"></a>个人 skills vs 项目 skills</h4><table><thead><tr><th>位置</th><th>作用范围</th><th>示例</th></tr></thead><tbody><tr><td><code>~/.claude/skills/</code></td><td>跨所有项目</td><td>你个人的代码审查流程</td></tr><tr><td><code>项目/.claude/skills/</code></td><td>仅当前项目</td><td>项目特定的部署流程</td></tr></tbody></table><hr><h3 id="3-4-agents-文件夹：专业化的子角色"><a href="#3-4-agents-文件夹：专业化的子角色" class="headerlink" title="3.4 agents/ 文件夹：专业化的子角色"></a>3.4 agents/ 文件夹：专业化的子角色</h3><h4 id="什么时候需要-agents？"><a href="#什么时候需要-agents？" class="headerlink" title="什么时候需要 agents？"></a>什么时候需要 agents？</h4><p>有些任务足够复杂，值得用一个<strong>独立角色</strong>来处理——它有自己的系统提示词、自己的工具限制、甚至可以用不同的模型。</p><p>比如代码审查：你不想让主会话被几千行中间探索过程淹没。你可以定义一个 <code>code-reviewer</code> agent，让它在自己的”小房间”里做审查，完成后只把结论告诉你。</p><h4 id="agent-的配置格式"><a href="#agent-的配置格式" class="headerlink" title="agent 的配置格式"></a>agent 的配置格式</h4><p>每个 agent 是一个 Markdown 文件，放在 <code>.claude/agents/</code> 下：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">.claude/agents/├── code<span class="token punctuation">-</span>reviewer.md     <span class="token comment"># 代码审查专家</span>└── security<span class="token punctuation">-</span>auditor.md  <span class="token comment"># 安全审计专家</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><code>code-reviewer.md</code> 的内容：</p><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token front-matter-block"><span class="token punctuation">---</span><span class="token front-matter yaml language-yaml"><span class="token key atrule">name</span><span class="token punctuation">:</span> code<span class="token punctuation">-</span>reviewer<span class="token key atrule">description</span><span class="token punctuation">:</span> 专业代码审查员。在审查 PR、检查 bug、  或合并前验证实现时主动使用。<span class="token key atrule">model</span><span class="token punctuation">:</span> sonnet<span class="token key atrule">tools</span><span class="token punctuation">:</span> Read<span class="token punctuation">,</span> Grep<span class="token punctuation">,</span> Glob</span><span class="token punctuation">---</span></span>你是一位资深代码审查员，专注于正确性和可维护性。审查代码时：<span class="token list punctuation">-</span> 标记 bug，不只是风格问题<span class="token list punctuation">-</span> 给出具体的修复建议，不说空话<span class="token list punctuation">-</span> 检查边界情况和错误处理的遗漏<span class="token list punctuation">-</span> 只在大规模场景下才提性能问题<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>关键字段：</strong></p><table><thead><tr><th>字段</th><th>作用</th><th>说明</th></tr></thead><tbody><tr><td><code>name</code></td><td>agent 名称</td><td>Claude 根据这个识别</td></tr><tr><td><code>description</code></td><td>什么时候调用</td><td>类似 skill 的 description</td></tr><tr><td><code>model</code></td><td>使用的模型</td><td><code>haiku</code>（便宜快速）、<code>sonnet</code>（均衡）、<code>opus</code>（最强）</td></tr><tr><td><code>tools</code></td><td>允许使用的工具</td><td>限制 agent 的能力范围</td></tr></tbody></table><h4 id="为什么要限制工具？"><a href="#为什么要限制工具？" class="headerlink" title="为什么要限制工具？"></a>为什么要限制工具？</h4><p>安全审计员只需要<strong>读</strong>代码，不需要<strong>写</strong>代码。把 <code>tools</code> 限制为 <code>Read, Grep, Glob</code>，就确保它不会意外修改任何文件。</p><p>这种限制是<strong>有意为之</strong>的——不同的角色，不同的权限。就像公司里，审计部门有查看权限，但没有操作权限。</p><h4 id="model-字段的意义"><a href="#model-字段的意义" class="headerlink" title="model 字段的意义"></a>model 字段的意义</h4><p><code>model</code> 字段让你可以为不同任务选择合适的模型：</p><table><thead><tr><th>模型</th><th>适合做什么</th><th>成本</th></tr></thead><tbody><tr><td><strong>haiku</strong></td><td>只读的探索任务、简单的搜索</td><td>最便宜</td></tr><tr><td><strong>sonnet</strong></td><td>代码审查、一般开发任务</td><td>中等</td></tr><tr><td><strong>opus</strong></td><td>复杂架构设计、关键决策</td><td>最贵</td></tr></tbody></table><p>把简单任务交给 haiku，复杂任务交给 opus，既省钱又高效。</p><h4 id="个人-agents"><a href="#个人-agents" class="headerlink" title="个人 agents"></a>个人 agents</h4><p>跟 skills 一样，agents 也分项目级和全局级：</p><table><thead><tr><th>位置</th><th>作用范围</th></tr></thead><tbody><tr><td><code>~/.claude/agents/</code></td><td>跨所有项目</td></tr><tr><td><code>项目/.claude/agents/</code></td><td>仅当前项目</td></tr></tbody></table><hr><h3 id="3-5-Auto-Memory：AI-的”自动笔记本”"><a href="#3-5-Auto-Memory：AI-的”自动笔记本”" class="headerlink" title="3.5 Auto Memory：AI 的”自动笔记本”"></a>3.5 Auto Memory：AI 的”自动笔记本”</h3><h4 id="CLAUDE-md-vs-Auto-Memory"><a href="#CLAUDE-md-vs-Auto-Memory" class="headerlink" title="CLAUDE.md vs Auto Memory"></a>CLAUDE.md vs Auto Memory</h4><table><thead><tr><th></th><th>CLAUDE.md</th><th>Auto Memory</th></tr></thead><tbody><tr><td><strong>谁写的</strong></td><td>你手动写</td><td>AI 自己记</td></tr><tr><td><strong>加载方式</strong></td><td>每次会话全量加载</td><td>先读索引，按需加载</td></tr><tr><td><strong>优先级</strong></td><td>高</td><td>中</td></tr><tr><td><strong>内容</strong></td><td>项目规范、技术栈</td><td>你的偏好、踩过的坑、项目决策</td></tr></tbody></table><p>如果说 CLAUDE.md 是<strong>你主动立下的规矩</strong>，Auto Memory 就是 <strong>AI 在干活过程中默默记下的笔记</strong>。你没写进 CLAUDE.md 的习惯、反馈、项目踩坑，AI 会自动记录。</p><h4 id="如何启用？"><a href="#如何启用？" class="headerlink" title="如何启用？"></a>如何启用？</h4><p>在 Claude Code 会话中输入：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">/memory<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>在弹出的菜单中选择”启用 Auto Memory”。启用后，AI 会自动开始记录。</p><h4 id="记录的内容分四类"><a href="#记录的内容分四类" class="headerlink" title="记录的内容分四类"></a>记录的内容分四类</h4><table><thead><tr><th>类型</th><th>含义</th><th>举例</th></tr></thead><tbody><tr><td><code>user</code></td><td>关于你</td><td>“用户不喜欢深色 UI”、”用户偏好函数式编程”</td></tr><tr><td><code>feedback</code></td><td>你给过的反馈</td><td>“不要这样做”、”对，就这样”</td></tr><tr><td><code>project</code></td><td>项目相关</td><td>“决定用 Redis 做缓存”、”数据库迁移脚本有问题”</td></tr><tr><td><code>reference</code></td><td>外部资源索引</td><td>“设计文档在 docs/design.md”</td></tr></tbody></table><h4 id="使用手感"><a href="#使用手感" class="headerlink" title="使用手感"></a>使用手感</h4><ul><li>它<strong>只在当前项目生效</strong>，换项目需重新积累</li><li>不会每次把所有记忆全加载——只读一份 <code>memory.md</code> 索引，<strong>遇到具体问题才去读对应的子文件</strong>，占 token 很少</li><li>随时按 <code>Ctrl+O</code> 查看被调用过的记忆内容（vscode中使用可能快捷键被占用）</li><li>记错了？直接跟 AI 说”忘掉刚才说的不喜欢深色主题”，它会自己删掉</li></ul><blockquote><p>💡 <strong>本质认知</strong>：agent 的所有”记忆”，本质上都是在合适的时候向大模型注入压缩过的上下文。CLAUDE.md 和 Auto Memory 只是组织方式不同。</p></blockquote><hr><h3 id="3-6-自建参考文档：渐进式披露"><a href="#3-6-自建参考文档：渐进式披露" class="headerlink" title="3.6 自建参考文档：渐进式披露"></a>3.6 自建参考文档：渐进式披露</h3><p>有些信息不适合全部塞进 CLAUDE.md（太长、太专门），但 AI 需要的时候必须能查到。</p><p><strong>解决方案：创建独立的参考文档，在 CLAUDE.md 中用指引告诉 AI 什么时候去读。</strong></p><p>举个例子，假设你做一个产品，需要：</p><ul><li>品牌视觉规范 → <code>docs/brand-visual.md</code></li><li>产品文案风格 → <code>docs/copywriting-style.md</code></li><li>API 约定 → <code>docs/api-conventions.md</code></li></ul><p>在 CLAUDE.md 中加上指引：</p><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token title important"><span class="token punctuation">##</span> 外部参考文档</span><span class="token list punctuation">-</span> 修改前端视觉、调颜色、调间距时 → 必读 <span class="token code-snippet code keyword">`docs/brand-visual.md`</span><span class="token list punctuation">-</span> 写产品文案、按钮文字、提示语时 → 必读 <span class="token code-snippet code keyword">`docs/copywriting-style.md`</span><span class="token list punctuation">-</span> 写 API、定义返回格式时 → 必读 <span class="token code-snippet code keyword">`docs/api-conventions.md`</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>效果：AI 只在”需要的时候”才去读完整文档。既保证了准确性，又不浪费 token。</p><p>这就是”渐进式披露”——<strong>平时不加载，需要时才加载。</strong></p><h4 id="三层记忆体系总览"><a href="#三层记忆体系总览" class="headerlink" title="三层记忆体系总览"></a>三层记忆体系总览</h4><p>到这里，Claude Code 的三层记忆体系就完整了：</p><table><thead><tr><th>层</th><th>位置</th><th>优先级</th><th>加载方式</th><th>谁在维护</th></tr></thead><tbody><tr><td>第一层</td><td>CLAUDE.md（三级）</td><td>高</td><td>会话启动全量加载</td><td>你手动维护</td></tr><tr><td>第二层</td><td>Auto Memory</td><td>中</td><td>先读索引、按需读子文件</td><td>AI 自己写、你校对</td></tr><tr><td>第三层</td><td>参考文档</td><td>按需</td><td>遇到对应任务才读</td><td>你手动维护</td></tr></tbody></table><hr><h2 id="第四部分：配置最佳实践"><a href="#第四部分：配置最佳实践" class="headerlink" title="第四部分：配置最佳实践"></a>第四部分：配置最佳实践</h2><p>理论讲完了，这一部分聊点实际的：新手怎么一步步搭起来？常见的坑有哪些？团队协作怎么分工？</p><h3 id="4-1-新手入门：5-步走"><a href="#4-1-新手入门：5-步走" class="headerlink" title="4.1 新手入门：5 步走"></a>4.1 新手入门：5 步走</h3><p>如果你从零开始，按这个顺序来，每一步都有明确目标：</p><p><strong>第 1 步：用 <code>/init</code> 生成 CLAUDE.md</strong></p><p>在项目根目录启动 Claude Code，输入 <code>/init</code>。它会自动扫描你的项目结构、技术栈，生成一份 CLAUDE.md 初稿。</p><p>拿到初稿后，精简到核心内容：常用命令、架构决策、编码规范、注意事项。控制在 200 行以内。</p><p><strong>第 2 步：配置 settings.json 的基础权限</strong></p><p>创建 <code>.claude/settings.json</code>，至少做两件事：</p><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"$schema"</span><span class="token operator">:</span> <span class="token string">"https://json.schemastore.org/claude-code-settings.json"</span><span class="token punctuation">,</span>  <span class="token property">"permissions"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"allow"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token string">"Read"</span><span class="token punctuation">,</span>      <span class="token string">"Write"</span><span class="token punctuation">,</span>      <span class="token string">"Edit"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(npm run *)"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(git status)"</span><span class="token punctuation">,</span>      <span class="token string">"Bash(git diff *)"</span>    <span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"deny"</span><span class="token operator">:</span> <span class="token punctuation">[</span>      <span class="token string">"Bash(rm -rf *)"</span><span class="token punctuation">,</span>      <span class="token string">"Read(./.env)"</span>    <span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>允许常用操作，禁止危险操作。其余的让它问你。</p><p><strong>第 3 步：添加 .claudeignore</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">node_modules/dist/build/.next/<span class="token important">*.log</span>.env.env.*.DS_Store<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>排除不需要的文件，省 token、防泄露。</p><p><strong>第 4 步：项目增长后，拆分 rules/</strong></p><p>当 CLAUDE.md 超过 100 行，按关注点拆分到 <code>.claude/rules/</code>。先拆最臃肿的部分，不用一次拆完。</p><p><strong>第 5 步：添加全局 <code>~/.claude/CLAUDE.md</code></strong></p><p>写下你的个人偏好，下面是我个人使用的一个例子：</p><pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown"><span class="token title important"><span class="token punctuation">#</span> 全局个人偏好</span><span class="token list punctuation">-</span> <span class="token bold"><span class="token punctuation">**</span><span class="token content">当前日期</span><span class="token punctuation">**</span></span>：2026-07-06<span class="token title important"><span class="token punctuation">##</span> 语言与沟通</span><span class="token list punctuation">-</span> 默认使用中文回复，用户要求英文时除外。<span class="token list punctuation">-</span> 代码、注释、文档、提交信息必须使用英文。<span class="token list punctuation">-</span> 严格遵循 Go 语言习惯（idiomatic Go），优先参考 Effective Go 和 Go Proverbs。<span class="token title important"><span class="token punctuation">##</span> Go 语言核心原则</span><span class="token list punctuation">-</span> 优先使用标准库（stdlib），仅在必要时引入第三方库。<span class="token list punctuation">-</span> 错误处理必须显式：不要忽略错误，使用 <span class="token code-snippet code keyword">`errors`</span> 包或自定义 error 类型。<span class="token list punctuation">-</span> 并发安全：优先使用 channels 和 context 管理 goroutine。<span class="token list punctuation">-</span> 性能优先：避免不必要的分配，合理使用 sync 包和 pooling。<span class="token list punctuation">-</span> 代码风格：简洁、可读、明确（clear is better than clever）。<span class="token list punctuation">-</span> 命名规范：使用驼峰（exported）/小写（unexported），接口名以 "er" 结尾（如 Reader）。<span class="token title important"><span class="token punctuation">##</span> 项目结构与规范</span><span class="token list punctuation">-</span> 采用标准 Go 项目布局：  <span class="token list punctuation">-</span> <span class="token code-snippet code keyword">`cmd/`</span>：可执行入口  <span class="token list punctuation">-</span> <span class="token code-snippet code keyword">`internal/`</span>：私有代码  <span class="token list punctuation">-</span> <span class="token code-snippet code keyword">`pkg/`</span>：可复用包（谨慎使用）  <span class="token list punctuation">-</span> <span class="token code-snippet code keyword">`api/`</span>、<span class="token code-snippet code keyword">`config/`</span>、<span class="token code-snippet code keyword">`pkg/`</span> 等按需组织<span class="token list punctuation">-</span> 模块管理：始终使用 Go Modules（go.mod），保持 go version 最新稳定版。<span class="token list punctuation">-</span> 测试：优先 table-driven tests，使用 <span class="token code-snippet code keyword">`testing`</span> 和 <span class="token code-snippet code keyword">`testify`</span>（如果项目已引入）。<span class="token title important"><span class="token punctuation">##</span> 工具与命令</span><span class="token list punctuation">-</span> Build：<span class="token code-snippet code keyword">`go build ./...`</span><span class="token list punctuation">-</span> Test：<span class="token code-snippet code keyword">`go test ./... -race -count=1`</span><span class="token list punctuation">-</span> Lint：<span class="token code-snippet code keyword">`golangci-lint run`</span><span class="token list punctuation">-</span> Format：<span class="token code-snippet code keyword">`go fmt ./...`</span> 或 <span class="token code-snippet code keyword">`gofumpt`</span><span class="token list punctuation">-</span> Vet：<span class="token code-snippet code keyword">`go vet ./...`</span><span class="token list punctuation">-</span> 依赖：<span class="token code-snippet code keyword">`go mod tidy &amp;&amp; go mod verify`</span><span class="token title important"><span class="token punctuation">##</span> Git 与提交</span><span class="token list punctuation">-</span> 使用 Conventional Commits：<span class="token code-snippet code keyword">`feat: `</span>、<span class="token code-snippet code keyword">`fix: `</span>、<span class="token code-snippet code keyword">`refactor: `</span> 等<span class="token list punctuation">-</span> 提交信息清晰、原子化。<span class="token title important"><span class="token punctuation">##</span> 最佳实践</span><span class="token list punctuation">-</span> 始终处理 context.Context 以支持取消和超时。<span class="token list punctuation">-</span> 避免全局变量，优先依赖注入。<span class="token list punctuation">-</span> 接口设计小而专一。<span class="token list punctuation">-</span> 生产就绪：添加健康检查、优雅关闭、结构化日志（推荐 zap 或 slog）。<span class="token list punctuation">-</span> 安全性：验证输入、避免 sql 注入、使用最新依赖。<span class="token title important"><span class="token punctuation">##</span> 关于我</span><span class="token list punctuation">-</span> 我是一名 Go 后端工程师，专注于高性能、并发系统、微服务和 CLI 工具。<span class="token list punctuation">-</span> 偏好干净架构（Clean Architecture）和领域驱动设计（DDD）思想。<span class="token list punctuation">-</span> 重视可测试性、可观测性和可维护性。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这 5 步做完，你就拥有了一套能覆盖 95% 项目的配置。剩下的 skills、agents、hooks，等你有具体的重复工作流时再加。</p><h3 id="4-2-配置避坑指南"><a href="#4-2-配置避坑指南" class="headerlink" title="4.2 配置避坑指南"></a>4.2 配置避坑指南</h3><h4 id="坑-1：CLAUDE-md-写成长篇小说"><a href="#坑-1：CLAUDE-md-写成长篇小说" class="headerlink" title="坑 1：CLAUDE.md 写成长篇小说"></a>坑 1：CLAUDE.md 写成长篇小说</h4><p><strong>症状</strong>：CLAUDE.md 超过 300 行，里面有大段的理论解释、完整的 API 文档、重复的代码示例。</p><p><strong>后果</strong>：AI 的指令遵循度下降，上下文被大量消耗，真正重要的指令被淹没。</p><p><strong>解决</strong>：</p><ul><li>控制在 200 行以内</li><li>只写”做什么”，不写”为什么”</li><li>详细文档用 <code>@docs/xxx.md</code> 引用</li></ul><h4 id="坑-2：安全-hook-用退出码-1"><a href="#坑-2：安全-hook-用退出码-1" class="headerlink" title="坑 2：安全 hook 用退出码 1"></a>坑 2：安全 hook 用退出码 1</h4><p><strong>症状</strong>：写了 PreToolUse hook 阻止危险命令，但用 <code>exit 1</code>。</p><p><strong>后果</strong>：危险命令照样执行。退出码 1 只记录日志，不阻止执行。</p><p><strong>解决</strong>：安全相关的 hook <strong>必须</strong> 用 <code>exit 2</code>。</p><h4 id="坑-3：权限设置过于宽松"><a href="#坑-3：权限设置过于宽松" class="headerlink" title="坑 3：权限设置过于宽松"></a>坑 3：权限设置过于宽松</h4><p><strong>症状</strong>：allow 列表里有 <code>Bash(*)</code>，等于什么命令都能跑。</p><p><strong>后果</strong>：AI 可能执行你没想到的危险操作，而且不会问你。</p><p><strong>解决</strong>：</p><ul><li>allow 列表只放你<strong>确定安全</strong>的操作</li><li>不确定的就放灰色地带，让它问你</li><li>deny 列表至少包含 <code>rm -rf</code>、<code>.env</code> 读取</li></ul><h4 id="坑-4：忘记-claudeignore"><a href="#坑-4：忘记-claudeignore" class="headerlink" title="坑 4：忘记 .claudeignore"></a>坑 4：忘记 .claudeignore</h4><p><strong>症状</strong>：没有 .claudeignore，AI 扫描了整个 node_modules。</p><p><strong>后果</strong>：上下文被垃圾信息填满，token 消耗暴增，还可能读到敏感文件。</p><p><strong>解决</strong>：项目第一天就创建 .claudeignore。</p><h4 id="坑-5：Stop-hook-无限循环"><a href="#坑-5：Stop-hook-无限循环" class="headerlink" title="坑 5：Stop hook 无限循环"></a>坑 5：Stop hook 无限循环</h4><p><strong>症状</strong>：Stop hook 跑测试，测试失败就阻止 AI 完成。但 AI 重试时又触发 hook，又阻止……</p><p><strong>后果</strong>：无限循环，Claude Code 卡死。</p><p><strong>解决</strong>：在 hook 脚本中检查 <code>stop_hook_active</code> 标志，第二次触发时放行。</p><h3 id="4-3-团队协作建议"><a href="#4-3-团队协作建议" class="headerlink" title="4.3 团队协作建议"></a>4.3 团队协作建议</h3><h4 id="什么提交到-Git？"><a href="#什么提交到-Git？" class="headerlink" title="什么提交到 Git？"></a>什么提交到 Git？</h4><table><thead><tr><th>文件</th><th>是否提交</th><th>原因</th></tr></thead><tbody><tr><td><code>CLAUDE.md</code></td><td>✅ 提交</td><td>团队共识</td></tr><tr><td><code>.claude/settings.json</code></td><td>✅ 提交</td><td>团队权限</td></tr><tr><td><code>.claude/rules/</code></td><td>✅ 提交</td><td>团队规范</td></tr><tr><td><code>.claude/skills/</code></td><td>✅ 提交</td><td>团队工作流</td></tr><tr><td><code>.claude/agents/</code></td><td>✅ 提交</td><td>团队角色</td></tr><tr><td><code>.claude/hooks/</code></td><td>⚠️ 看情况</td><td>如果脚本包含本地路径，不提交</td></tr><tr><td><code>.claudeignore</code></td><td>✅ 提交</td><td>团队共识</td></tr><tr><td><code>CLAUDE.local.md</code></td><td>❌ 不提交</td><td>个人偏好（自动 gitignore）</td></tr><tr><td><code>.claude/settings.local.json</code></td><td>❌ 不提交</td><td>个人权限（自动 gitignore）</td></tr></tbody></table><h4 id="分工维护"><a href="#分工维护" class="headerlink" title="分工维护"></a>分工维护</h4><p>大型项目中，一个人维护所有配置文件是不现实的。建议按领域分工：</p><ul><li><strong>前端负责人</strong>：维护 <code>rules/frontend.md</code></li><li><strong>后端负责人</strong>：维护 <code>rules/backend.md</code>、<code>rules/api-conventions.md</code></li><li><strong>测试负责人</strong>：维护 <code>rules/testing.md</code></li><li><strong>安全负责人</strong>：维护 <code>hooks/bash-firewall.sh</code>、<code>skills/security-review/</code></li></ul><p>每个文件有明确的 owner，改的时候不会冲突。</p><h3 id="4-4-性能优化建议"><a href="#4-4-性能优化建议" class="headerlink" title="4.4 性能优化建议"></a>4.4 性能优化建议</h3><h4 id="省-token"><a href="#省-token" class="headerlink" title="省 token"></a>省 token</h4><ol><li><strong>保持 CLAUDE.md 简洁</strong>：200 行以内</li><li><strong>用好 .claudeignore</strong>：排除大文件和无关文件</li><li><strong>路径作用域 rules/</strong>：只在需要时加载规则</li><li><strong>渐进式披露参考文档</strong>：不在 CLAUDE.md 里贴大段内容</li><li><strong>定期 <code>/compact</code></strong>：上下文超过 60% 就压缩</li></ol><h4 id="提速度"><a href="#提速度" class="headerlink" title="提速度"></a>提速度</h4><ol><li><strong>精确的 CLAUDE.md</strong>：AI 不需要花时间”探索”项目</li><li><strong><code>@</code> 引用文件</strong>：直接告诉 AI 看哪个文件，而不是让它自己找</li><li><strong>限制 agents/skills 的工具</strong>：只给必要的工具，减少决策空间</li><li><strong>用对模型</strong>：简单任务用 haiku，复杂任务用 opus</li></ol><hr><h2 id="第五部分：完整配置示例"><a href="#第五部分：完整配置示例" class="headerlink" title="第五部分：完整配置示例"></a>第五部分：完整配置示例</h2><p>理论讲完了，给你两套前后端可以直接抄的配置模板。基本内容上面都已经提到了，只不过是对上面内容进行总结打包，下面是具体配置项目结构，有需要可以关注我后台回复<code>cc</code>获取。</p><div style="display: flex; justify-content: space-around; gap: 20px;">  <img src="/images/claudecode/frontend.png" width="48%" alt="前端架构">  <img src="/images/claudecode/backend.png" width="48%" alt="后端架构"></div><p>此外我还打包了github上star比较多的常用的配置。</p><h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><h3 id="A-文件位置速查表"><a href="#A-文件位置速查表" class="headerlink" title="A. 文件位置速查表"></a>A. 文件位置速查表</h3><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">~/.claude/                        ← 全局配置├── CLAUDE.md                     <span class="token comment"># 全局指令（所有项目生效）</span>├── settings.json                 <span class="token comment"># 全局权限设置</span>├── skills/                       <span class="token comment"># 个人 skills（跨项目）</span>├── agents/                       <span class="token comment"># 个人 agents（跨项目）</span>└── projects/                     <span class="token comment"># 会话历史 + 自动记忆</span>项目根目录/├── CLAUDE.md                     <span class="token comment"># 项目指令（团队共享）</span>├── CLAUDE.local.md               <span class="token comment"># 个人覆盖（不进 Git）</span>├── .claudeignore                 <span class="token comment"># 忽略文件列表</span>│└── .claude/                      ← 项目级配置    ├── settings.json             <span class="token comment"># 团队权限设置</span>    ├── settings.local.json       <span class="token comment"># 个人权限（不进 Git）</span>    ├── rules/                    <span class="token comment"># 模块化规则</span>    ├── skills/                   <span class="token comment"># 项目级 skills</span>    ├── agents/                   <span class="token comment"># 项目级 agents</span>    └── hooks/                    <span class="token comment"># hook 脚本</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="B-常用命令速查表"><a href="#B-常用命令速查表" class="headerlink" title="B. 常用命令速查表"></a>B. 常用命令速查表</h3><table><thead><tr><th>命令</th><th>作用</th><th>快捷键</th></tr></thead><tbody><tr><td><code>/help</code></td><td>查看所有命令</td><td>-</td></tr><tr><td><code>/init</code></td><td>自动生成 CLAUDE.md</td><td>-</td></tr><tr><td><code>/memory</code></td><td>管理记忆和 CLAUDE.md</td><td><code>Ctrl+O</code> 查看记忆</td></tr><tr><td><code>/context</code></td><td>查看上下文使用情况</td><td>-</td></tr><tr><td><code>/compact</code></td><td>压缩上下文</td><td>-</td></tr><tr><td><code>/clear</code></td><td>清空对话</td><td>-</td></tr><tr><td><code>/model</code></td><td>切换模型</td><td>-</td></tr><tr><td><code>/rewind</code></td><td>回滚修改</td><td><code>Esc × 2</code></td></tr><tr><td><code>/review</code></td><td>代码审查</td><td>-</td></tr><tr><td><code>/cost</code></td><td>查看费用</td><td>-</td></tr><tr><td><code>/resume</code></td><td>恢复历史会话</td><td>-</td></tr><tr><td><code>/plan</code></td><td>进入规划模式</td><td><code>Shift+Tab</code> 切换</td></tr></tbody></table><p><strong>快捷键：</strong></p><table><thead><tr><th>快捷键</th><th>作用</th></tr></thead><tbody><tr><td><code>Enter</code></td><td>发送消息 / 确认</td></tr><tr><td><code>Shift + Enter</code></td><td>也是发送（不是换行！）</td></tr><tr><td><code>Option/Alt + Enter</code></td><td>换行（不发送）</td></tr><tr><td><code>Ctrl + C</code></td><td>中断当前操作</td></tr><tr><td><code>Esc</code></td><td>取消生成</td></tr><tr><td><code>Esc × 2</code></td><td>回滚界面</td></tr><tr><td><code>Shift + Tab</code></td><td>切换模式</td></tr><tr><td><code>Ctrl + B</code></td><td>后台运行</td></tr></tbody></table><h3 id="C-YAML-Frontmatter-速查"><a href="#C-YAML-Frontmatter-速查" class="headerlink" title="C. YAML Frontmatter 速查"></a>C. YAML Frontmatter 速查</h3><p><strong>rules/ 文件：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">---</span><span class="token key atrule">paths</span><span class="token punctuation">:</span>  <span class="token punctuation">-</span> <span class="token string">"src/api/**/*.ts"</span>  <span class="token punctuation">-</span> <span class="token string">"src/handlers/**/*.ts"</span><span class="token punctuation">---</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>skills/SKILL.md：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">---</span><span class="token key atrule">name</span><span class="token punctuation">:</span> security<span class="token punctuation">-</span>review<span class="token key atrule">description</span><span class="token punctuation">:</span> 安全审查，在用户提到安全时使用<span class="token key atrule">allowed-tools</span><span class="token punctuation">:</span> Read<span class="token punctuation">,</span> Grep<span class="token punctuation">,</span> Glob<span class="token punctuation">---</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><strong>agents/ 文件：</strong></p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">---</span><span class="token key atrule">name</span><span class="token punctuation">:</span> code<span class="token punctuation">-</span>reviewer<span class="token key atrule">description</span><span class="token punctuation">:</span> 代码审查专家<span class="token key atrule">model</span><span class="token punctuation">:</span> sonnet<span class="token key atrule">tools</span><span class="token punctuation">:</span> Read<span class="token punctuation">,</span> Grep<span class="token punctuation">,</span> Glob<span class="token punctuation">---</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="D-settings-json-配置项速查"><a href="#D-settings-json-配置项速查" class="headerlink" title="D. settings.json 配置项速查"></a>D. settings.json 配置项速查</h3><pre class="line-numbers language-json" data-language="json"><code class="language-json"><span class="token punctuation">{</span>  <span class="token property">"$schema"</span><span class="token operator">:</span> <span class="token string">"https://json.schemastore.org/claude-code-settings.json"</span><span class="token punctuation">,</span>  <span class="token comment">// 权限控制</span>  <span class="token property">"permissions"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"allow"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"Read"</span><span class="token punctuation">,</span> <span class="token string">"Write"</span><span class="token punctuation">,</span> <span class="token string">"Bash(npm run *)"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"deny"</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"Bash(rm -rf *)"</span><span class="token punctuation">,</span> <span class="token string">"Read(./.env)"</span><span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">,</span>  <span class="token comment">// 默认模型</span>  <span class="token property">"model"</span><span class="token operator">:</span> <span class="token string">"sonnet"</span><span class="token punctuation">,</span>  <span class="token comment">// 自动压缩阈值</span>  <span class="token property">"autoCompactThreshold"</span><span class="token operator">:</span> <span class="token number">80</span><span class="token punctuation">,</span>  <span class="token comment">// 环境变量</span>  <span class="token property">"env"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"NODE_ENV"</span><span class="token operator">:</span> <span class="token string">"development"</span>  <span class="token punctuation">}</span><span class="token punctuation">,</span>  <span class="token comment">// Hooks</span>  <span class="token property">"hooks"</span><span class="token operator">:</span> <span class="token punctuation">{</span>    <span class="token property">"PreToolUse"</span><span class="token operator">:</span> <span class="token punctuation">[</span>...<span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"PostToolUse"</span><span class="token operator">:</span> <span class="token punctuation">[</span>...<span class="token punctuation">]</span><span class="token punctuation">,</span>    <span class="token property">"Stop"</span><span class="token operator">:</span> <span class="token punctuation">[</span>...<span class="token punctuation">]</span>  <span class="token punctuation">}</span><span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><hr><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>回到文章开头的问题：怎么让 AI 真正”懂”你的项目？</p><p>答案就是<strong>配置</strong>。</p><ul><li><strong>CLAUDE.md</strong> 告诉 AI 项目是什么、怎么做</li><li><strong>settings.json</strong> 控制 AI 能做什么、不能做什么</li><li><strong>rules/</strong> 把规则拆分成模块，按需加载</li><li><strong>hooks/</strong> 让规则从”建议”变成”强制执行”</li><li><strong>skills/</strong> 打包重复的工作流</li><li><strong>agents/</strong> 定义专业的子角色</li><li><strong>Auto Memory</strong> 让 AI 自己记住你的偏好</li></ul><p>配置不是一蹴而就的。从小处开始——先写好 CLAUDE.md，配好基本权限。用一段时间后，你会发现”这里可以自动化”、”那个规则应该拆出来”。逐步迭代，配置会越来越完善。</p><p><strong>好的配置，就像好的基础设施——一旦搭好，每天都会给你带来回报。</strong></p><p>现在，打开你的项目，开始配置吧。</p><hr><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>本文内容参考了以下资料，感谢各位大佬的分享：</p><ol><li><p><strong>Claude Code 官方文档</strong></p><ul><li><a href="https://code.claude.com/docs/zh-CN/how-claude-code-works">https://code.claude.com/docs/zh-CN/how-claude-code-works</a></li></ul></li><li><p><strong>尚硅谷 AI Coding 教程</strong></p><ul><li>零基础实战教程，涵盖 Claude Code 深度使用与进阶技巧</li><li><a href="https://www.bilibili.com/video/BV1RPET6tEp2/?share_source=copy_web&amp;vd_source=1c112747a1b05592cc7e39e91a3d66aa">https://www.bilibili.com/video/BV1RPET6tEp2/?share_source=copy_web&amp;vd_source=1c112747a1b05592cc7e39e91a3d66aa</a></li></ul></li><li><p><strong>@akshay_pachaar的twitter推文</strong></p><ul><li>原文：<a href="https://x.com/akshay_pachaar/status/2035341800739877091">https://x.com/akshay_pachaar/status/2035341800739877091</a></li><li>详细解析 CLAUDE.md、自定义命令、skills、agents 和 permissions 的配置方法</li></ul></li></ol>]]>
    </content>
    <id>https://welcomemonth.github.io/2026/03/07/claude%20code/</id>
    <link href="https://welcomemonth.github.io/2026/03/07/claude%20code/"/>
    <published>2026-03-07T08:42:38.000Z</published>
    <summary>
      <![CDATA[<h1 id="Claude-Code-配置全解析-让-AI-真正懂你的项目"><a href="#Claude-Code-配置全解析-让-AI-真正懂你的项目" class="headerlink" title="Claude Code 配置全解析:让 AI 真正懂你的项目">]]>
    </summary>
    <title>Claude Code 配置全解析:让 AI 真正懂你的项目</title>
    <updated>2026-04-18T08:00:00.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>ZhangZhengYu</name>
    </author>
    <category term="技术" scheme="https://welcomemonth.github.io/categories/%E6%8A%80%E6%9C%AF/"/>
    <category term="博客搭建" scheme="https://welcomemonth.github.io/categories/%E6%8A%80%E6%9C%AF/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"/>
    <category term="教程" scheme="https://welcomemonth.github.io/tags/%E6%95%99%E7%A8%8B/"/>
    <category term="blog" scheme="https://welcomemonth.github.io/tags/blog/"/>
    <category term="Hexo" scheme="https://welcomemonth.github.io/tags/Hexo/"/>
    <category term="github pages" scheme="https://welcomemonth.github.io/tags/github-pages/"/>
    <content>
      <![CDATA[<p>参考内容：<br><a href="https://hexo.io/">Hexo官网</a><br><a href="https://hexo.io/docs/">官方文档</a><br><a href="https://hexo.io/docs/troubleshooting.html">使用问题</a><br><a href="https://github.com/hexojs/hexo/issues">GitHub Issues</a></p><h2 id="一、前期准备（必须安装的环境）"><a href="#一、前期准备（必须安装的环境）" class="headerlink" title="一、前期准备（必须安装的环境）"></a>一、前期准备（必须安装的环境）</h2><p>1、Node.js（Hexo依赖）</p><ul><li>去官网下载合适的LTS版本：<a href="https://nodejs.org/">https://nodejs.org</a> 建议使用18版本以上。</li><li>具体安装使用自行搜索，安装完成后打开终端进行验证：</li></ul><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">node</span> <span class="token parameter variable">-v</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>2、Git安装</p><ul><li>官网：<a href="https://git-scm.com/">https://git-scm.com</a></li><li>安装后验证：<code>git --version</code></li></ul><p>3、<a href="#my-anchor">github账号</a></p><ul><li>如果没有则先注册一个，记住你账户的用户名，<code>${username}.github.io</code>就是你的个人站点。<a href="#my-anchor">点击这里跳转到仓库创建</a></li></ul><h2 id="二、-全局安装Hexo命令行工具"><a href="#二、-全局安装Hexo命令行工具" class="headerlink" title="二、 全局安装Hexo命令行工具"></a>二、 全局安装Hexo命令行工具</h2><p>由于国内的网络问题，因此推荐使用<code>pnpm</code>，如果没有安装，也可以直接将<code>pnpm</code>替换为<code>npm</code>。</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">npm</span> i <span class="token parameter variable">-g</span> <span class="token function">pnpm</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>1、全局安装Hexo命令行工具并验证</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">pnpm</span> i <span class="token parameter variable">-g</span> hexo-cli<span class="token comment"># 有时候win下需要重启终端</span>hexo <span class="token parameter variable">-v</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>2、创建博客目录并初始化</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">hexo init blog<span class="token builtin class-name">cd</span> blog<span class="token function">pnpm</span> <span class="token function">install</span> <span class="token comment"># 如果安装失败，并提示Run "pnpm approve-builds" to pick which dependencies should be allowed to run scripts.直接运行</span><span class="token function">pnpm</span> approve-builds<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>解释：hexo init会在执行命令的文件夹中生成标准的Hexo目录结构，使用pnpm安装依赖。</p><p>3、本地预览</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">hexo server <span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>浏览器直接访问<code>http://localhost:4000</code>，可以看到默认的landscape主题就安装成功了。</p><p><img src="/images/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/1787106844625.png" alt="landscape主题图"></p><h2 id="三、安装并启用-Matery-主题"><a href="#三、安装并启用-Matery-主题" class="headerlink" title="三、安装并启用 Matery 主题"></a>三、安装并启用 Matery 主题</h2><p>1、下载主题</p><blockquote><p>推荐 git clone，方便后续更新，也可以直接去github上下载</p></blockquote><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token builtin class-name">cd</span> themes<span class="token function">git</span> clone https://github.com/blinkfox/hexo-theme-matery.git<span class="token comment"># 重命名为更短的名字</span><span class="token function">mv</span> hexo-theme-matery matery<span class="token builtin class-name">cd</span> <span class="token punctuation">..</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>2、修改hexo配置，启用matery</p><p>打开blog目录下的<code>_config.yml</code>文件，找到<code>theme:</code>一行，改为：</p><pre class="line-numbers language-text" data-language="text"><code class="language-text"># 和上面文件夹的名称保持一致theme: matery<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>想要做什么修改，具体配置的内容参考官方文档：<code>https://hexo.io/zh-cn/docs/configuration.html#%E7%BD%91%E7%AB%99</code></p><p>例如修改语言为中文：</p><pre class="line-numbers language-text" data-language="text"><code class="language-text">language: zh-CN<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>3、创建主题需要的页面</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">hexo new page categorieshexo new page tagshexo new page about<span class="token comment"># 可选</span>hexo new page friendshexo new page contact<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>然后编辑对应文件，即将layout与matery中对应上，下面是about的修改，其他的几个创建的页面也同理：</p><pre class="line-numbers language-text" data-language="text"><code class="language-text">---title: aboutdate: 2024-06-01 16:22:00type: "about"layout: "about"---<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这个<code>hexo new</code>命令只是在source目录下创建了一个文件夹，并创建了一个<code>index.md</code>文件，在hexo的规则中，source文件夹下的任何index.md最终都会生成一个独立的HTML网页，路径就是文件夹的名字，比如<code>about/index</code>.md 最终的访问地址就是<code>localhost:4000/about</code>，具体这个页面的渲染是依赖matery主题，在主题文件夹中也有_config.yml配置文件，搜索<code>menu</code>就会看到具体的路由配置，如果没有在hexo的source中创建对应的文件夹，当跳转的时候就会提示404页面。具体该页面的渲染内容，例如about页面，则是在matery中的layout下<code>about.ejs</code>中。</p><p>4、目录中的代码高亮配置：<br>打开博客根目录的 _config.yml，把 Writing 部分的高亮相关内容改成下面这样：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token comment"># Writing</span><span class="token key atrule">new_post_name</span><span class="token punctuation">:</span> <span class="token punctuation">:</span>title.md<span class="token key atrule">default_layout</span><span class="token punctuation">:</span> post<span class="token key atrule">titlecase</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token key atrule">external_link</span><span class="token punctuation">:</span>  <span class="token key atrule">enable</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">field</span><span class="token punctuation">:</span> site  <span class="token key atrule">exclude</span><span class="token punctuation">:</span> <span class="token string">''</span><span class="token key atrule">filename_case</span><span class="token punctuation">:</span> <span class="token number">0</span><span class="token key atrule">render_drafts</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token key atrule">post_asset_folder</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token key atrule">relative_link</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token key atrule">future</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token comment"># ========== 代码高亮配置（Hexo 8 正确写法） ==========</span><span class="token key atrule">syntax_highlighter</span><span class="token punctuation">:</span> prismjs<span class="token key atrule">highlight</span><span class="token punctuation">:</span>  <span class="token key atrule">enable</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span class="token key atrule">prismjs</span><span class="token punctuation">:</span>  <span class="token key atrule">enable</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">preprocess</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>      <span class="token comment"># 服务端预处理（推荐）</span>  <span class="token key atrule">line_number</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>     <span class="token comment"># 显示行号</span>  <span class="token key atrule">tab_replace</span><span class="token punctuation">:</span> <span class="token string">''</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>此外如果发现代码块收缩和下滑有一些样式冲突，则直接把：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">code</span><span class="token punctuation">:</span>  <span class="token key atrule">lang</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">copy</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>  <span class="token key atrule">shrink</span><span class="token punctuation">:</span> <span class="token boolean important">false</span>   <span class="token comment"># ← 先关掉收缩功能</span>  <span class="token key atrule">break</span><span class="token punctuation">:</span> <span class="token boolean important">false</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>5、推荐安装的插件：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">pnpm</span> <span class="token function">install</span> hexo-generator-search <span class="token parameter variable">--save</span>          <span class="token comment"># 站内搜索</span><span class="token function">pnpm</span> <span class="token function">install</span> hexo-wordcount <span class="token parameter variable">--save</span>                 <span class="token comment"># 字数统计</span><span class="token function">pnpm</span> <span class="token function">install</span> hexo-permalink-pinyin <span class="token parameter variable">--save</span>          <span class="token comment"># 中文链接转拼音（SEO 友好）</span><span class="token function">pnpm</span> <span class="token function">install</span> hexo-generator-feed <span class="token parameter variable">--save</span>            <span class="token comment"># RSS</span><span class="token comment"># 可选 emoji</span><span class="token function">pnpm</span> <span class="token function">install</span> hexo-filter-github-emojis <span class="token parameter variable">--save</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>6、主题个性化配置</p><p>打开 themes/matery/_config.yml，这里可以改：</p><ul><li>菜单、社交链接、头像、favicon</li><li>Banner 图片、背景图<br>评论系统（推荐 Gitalk 或 Valine）</li><li>不蒜子统计、Google Analytics 等</li><li>文章信息显示（字数、阅读时间等）</li></ul><p>7、本地预览主题效果</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">hexo cleanhexo generate   <span class="token comment"># 或 hexo g</span>hexo server     <span class="token comment"># 或 hexo s</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>访问<code>http://localhost:4000</code>查看效果。</p><h2 id="四、部署到GitHub-Pages"><a href="#四、部署到GitHub-Pages" class="headerlink" title="四、部署到GitHub Pages"></a>四、部署到GitHub Pages</h2><p>1、安装hexo部署插件</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"><span class="token function">pnpm</span> <span class="token function">install</span> hexo-deployer-git <span class="token parameter variable">--save</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>2、项目配置：</p><p>在根目录<code>_config.yml</code>末尾添加（把<code>username</code>换成你的 GitHub 用户名）：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token key atrule">deploy</span><span class="token punctuation">:</span>  <span class="token key atrule">type</span><span class="token punctuation">:</span> git  <span class="token key atrule">repo</span><span class="token punctuation">:</span> git@github.com<span class="token punctuation">:</span>username/username.github.io.git   <span class="token comment"># SSH 方式（推荐）</span>  <span class="token comment"># 或者 HTTPS：https://github.com/username/username.github.io.git</span>  <span class="token key atrule">branch</span><span class="token punctuation">:</span> main   <span class="token comment"># 或者 master，看你仓库默认分支</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>3、<a id="my-anchor" name="my-anchor">在 GitHub 创建仓库</a></p><p>如果之前你已经创建，则这一步可以取消。</p><ul><li>新建仓库，名字必须是 你的用户名.github.io（例如 zhangsan.github.io）。</li><li>选择 Public，不要勾选添加 README。</li></ul><p>4、部署</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">hexo cleanhexo deploy     <span class="token comment"># 或 hexo d</span><span class="token comment"># 也可以一次搞定：hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>第一次可能需要输入 GitHub 账号密码（或使用 Personal Access Token）。成功后等待 1～2 分钟，访问 <code>https://你的用户名.github.io</code> 即可看到网站。</p><p>使用这种方式是相当于把源码进行编译成静态网页推送到了这个仓库，本地的源码并没有推送上去，因此还需要再把本地代码进行保存推送。</p><h2 id="五、创建博客"><a href="#五、创建博客" class="headerlink" title="五、创建博客"></a>五、创建博客</h2><h3 id="新建文章"><a href="#新建文章" class="headerlink" title="新建文章"></a>新建文章</h3><p>hexo new “文章标题”</p><h1 id="编辑-source-posts-文章标题-md"><a href="#编辑-source-posts-文章标题-md" class="headerlink" title="编辑 source/_posts/文章标题.md"></a>编辑 source/_posts/文章标题.md</h1><h3 id="生成-本地预览"><a href="#生成-本地预览" class="headerlink" title="生成 + 本地预览"></a>生成 + 本地预览</h3><p>hexo clean &amp;&amp; hexo g &amp;&amp; hexo s</p><h3 id="部署上线"><a href="#部署上线" class="headerlink" title="部署上线"></a>部署上线</h3><p>hexo clean &amp;&amp; hexo d</p><p>文章 Front-matter 常用字段示例（Matery 支持很多扩展）：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml"><span class="token punctuation">---</span><span class="token key atrule">title</span><span class="token punctuation">:</span> 我的第一篇文章<span class="token key atrule">date</span><span class="token punctuation">:</span> <span class="token datetime number">2026-08-17 10:00:00</span><span class="token key atrule">categories</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>技术<span class="token punctuation">,</span> Hexo<span class="token punctuation">]</span><span class="token key atrule">tags</span><span class="token punctuation">:</span> <span class="token punctuation">[</span>博客<span class="token punctuation">,</span> Matery<span class="token punctuation">]</span><span class="token key atrule">img</span><span class="token punctuation">:</span> /images/cover.jpg          <span class="token comment"># 特色图片</span><span class="token key atrule">top</span><span class="token punctuation">:</span> <span class="token boolean important">true</span>                       <span class="token comment"># 置顶</span><span class="token key atrule">cover</span><span class="token punctuation">:</span> <span class="token boolean important">true</span><span class="token key atrule">summary</span><span class="token punctuation">:</span> 这是摘要<span class="token punctuation">...</span><span class="token punctuation">---</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="六、常见问题"><a href="#六、常见问题" class="headerlink" title="六、常见问题"></a>六、常见问题</h2><ul><li>图片显示不了：把图片放在<code>source/images/</code>下，用相对路径引用。</li><li>国内访问慢：可以配合 jsDelivr、Cloudflare 或把静态资源放到国内图床/OSS。</li><li>主题更新：进入主题目录<code>git pull</code>，注意备份自己改过的<code>_config.yml</code>。</li><li>自定义域名：在<code>GitHub</code>仓库 <code>Settings</code> → <code>Pages</code> 添加域名，并在<code>source/CNAME</code>文件写上你的域名。</li><li>评论系统：本站点使用的是twikoo，具体部署参考<code>https://twikoo.js.org/quick-start.html</code></li></ul>]]>
    </content>
    <id>https://welcomemonth.github.io/2024/08/18/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</id>
    <link href="https://welcomemonth.github.io/2024/08/18/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"/>
    <published>2024-08-18T07:00:00.000Z</published>
    <summary>
      <![CDATA[<p>参考内容：<br><a href="https://hexo.io/">Hexo官网</a><br><a href="https://hexo.io/docs/">官方文档</a><br><a href="https://hexo.io/docs/troubleshooti]]>
    </summary>
    <title>基于hexo+matery+GitHub pages搭建博客</title>
    <updated>2025-08-18T08:00:00.000Z</updated>
  </entry>
</feed>
